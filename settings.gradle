pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
    }
}
include ':sample', ':deeplinkdispatch', ':deeplinkdispatch-base', ':deeplinkdispatch-processor', ':sample-library', ':sample-benchmarkable-library', ':sample-benchmark'
